# 产品需求文档 (PRD)：多模块私有 Web 学习激励平台
## 1. 项目概述
### 1.1 项目背景与目标
本项目旨在利用私有服务器资源，为低年级学龄儿童打造一个全封闭、高隐私、可扩展的个性化 Web 学习平台。平台不仅提供多学科的学习与练习模块，更引入深度的游戏化成长激励机制（模拟 Minecraft 机制），将日常的枯燥练习转化为角色成长与战斗的动力，从而建立长期的正向反馈环。
### 1.2 目标用户
 * **核心使用者：** 小学低年级学生（如二年级阶段）。
 * **系统管理员：** 家长（负责后台配置、难度调整、系统维护）。
### 1.3 技术栈规划（参考）
 * **全栈框架：** Next.js (React) - 核心利用 App Router 与 API Routes 实现前后端一体化。
 * **前端 UI：** Tailwind CSS - 保证大颗粒度、高对比度的儿童友好型交互界面。
 * **数据库：** SQLite（初期单文件存储，便于轻量化部署与整盘备份）。
 * **部署环境：** Docker 容器化部署（运行于 Linux 服务器环境），内网通过局域网/跨设备访问，外网通过安全虚拟局域网（如 Tailscale）接入。
## 2. 核心架构与组件扩展性设计
为了支撑未来“多学科、多功能”的扩展需求，平台在架构设计上必须解耦“学习模块”与“核心激励系统”。
### 2.1 模块化分层架构
 1. **平台核心宿主 (Core Shell)：** 负责全局路由、用户认证（极简登录）、全局状态管理、导航框架以及底层数据库连接。
 2. **可插拔学习模块 (Pluggable Study Modules)：**
   * 每个学科（如：数学口算、语文听写、英语单词）作为一个独立的子模块或组件存在。
   * **标准化数据契约 (Data Contract)：** 所有学习模块在完成练习后，必须向核心系统统一提交标准的结算事件（Payload 包含：moduleId, duration, totalQuestions, correctCount, score）。
 3. **全局激励引擎 (Gamification Engine)：** 独立于学习模块之外。监听结算事件，根据得分和耗时转化为虚拟金币（Gold）和经验值（EXP），并更新游戏角色的属性。
### 2.2 前端组件扩展性规范
 * **动态路由设计：** 每一个新的学习功能作为 /modules/[module-name] 下的独立页面进行开发，互不干扰。
 * **复用型 UI 组件库：** 提炼出适合儿童操作的通用组件（如：吸顶倒计时组件、大字体数字输入框、通关结算弹窗、防误触虚拟小键盘等）。
## 3. 功能模块详细设计
### 3.1 核心功能一：数学口算练习模块（首发模块）
#### 3.1.1 页面布局与排版
 * **双列考卷流：** 页面主体采用左右双列网格布局（Grid），单页总题量固定为 50 道题。在 iPad/PC 端保持双列，在窄屏手机端自动降级为单列。
 * **视觉记忆还原：** 题目表现形式为 12 + 15 = (  )。输入框背景透明，仅保留下边框，或包裹在显眼的括号内，最大程度还原纸质试卷的视觉感。
 * **置顶毛玻璃倒计时：** 倒计时组件固定在屏幕最上方。时间根据后台配置的难易度动态变化（标准为 3 分钟或 4 分钟）。当时间剩余不足 60 秒时，数字变为红色闪烁，提供视觉压迫感。
#### 3.1.2 核心交互：多端无缝跳转逻辑
频繁点击输入框会严重破坏口算节奏，必须实现全键盘/全触屏的无缝焦点流转。
 * **PC/Mac 端（USB 数字小键盘）优化：**
   * 监听输入框的 KeyDown 事件。
   * 当孩子按下小键盘的 Enter（回车）或 Tab 键时，系统自动阻止默认行为，并将光标焦点（Focus）自动移至下一道题的输入框，同时自动执行 .select()（选中已有文本，方便直接覆盖修改）。
 * **iPad/触屏端优化：**
   * 所有输入框强制声明 inputmode="numeric"，确保在 iOS 设备上点击时，自动唤起系统原生的纯数字大颗粒键盘，而非全键盘。
   * **自动长度跳转（可选配置）：** 系统了解当前题目的正确答案位数（例如：答案是 42，位数为 2）。当检测到输入框内的字符长度达到该位数时，无需敲击任何按键，焦点自动跳到下一题。
#### 3.1.3 考试控制逻辑
 * **防刷新与状态同步：** 点击“开始考试”后，在 localStorage 中记录考试截止时间戳。即使孩子中途误触刷新页面，定时器和已填写的答案能立刻从本地缓存中恢复，防止考试作弊或中断。
 * **强制收卷：** 倒计时归零时，页面立即触发强制收卷。所有输入框变为 disabled（禁用）状态，阻止进一步输入，并自动向后端发送当前已填写的答案。
### 3.2 核心功能二：数据统计与错题回顾模块
#### 3.2.1 错题动态权重引擎
 * **数据落库：** 每次提交后，系统将错误的题型（如：进位加法、退位减法）以及具体的数字组合（如：17 + 8）存入 wrong_questions 错题本数据库。
 * **动态出题干预：** 在下一次动态生成 50 道题目的试卷时，后端题目生成算法会读取历史错题。根据错题频次提高该类题型的出题权重（例如：50道题中自动包含 10 道类似历史错题的变形题），实现精准复习。
#### 3.2.2 个人成长数据大盘
 * **可视化图表：** 面向家长和孩子展示近 7 日/30 日的“口算正确率曲线”和“平均单题耗时趋势”。
 * **里程碑记录：** 记录“历史最高分”、“最快通关时间”、“连续全对天数”等荣誉指标。
### 3.3 核心功能三：Minecraft 模拟成长激励模块
这是本平台长效留存的核心。将学科练习转化为游戏内资源的获取。
#### 3.3.1 角色属性系统 (Steve Profile)
 * 建立一个常驻的游戏角色状态（以 Steve 为原型），包含以下基础数据：
   * **等级 (Level) 与 经验值 (EXP)**
   * **虚拟金币余额 (Gold Coins)**
   * **战斗属性值：** 生命值 (HP)、攻击力 (ATK)、防御力 (DEF)。
   * **装备槽：** 武器栏、头盔、胸甲、护腿、靴子。
#### 3.3.2 练习转化机制（点石成金）
 * 当核心系统收到学习模块的结算事件时，根据以下公式转化奖励：
   * *基础奖励：* 每做对 1 道题获得 X 金币和 Y 经验。
   * *完美奖励：* 50道题全对（100分），触发“满分暴击”，额外赠送大额金币。
   * *速度奖励：* 在规定时间内提前完成，每提前 10 秒，额外奖励少量金币。
#### 3.3.3 全套装备武器商店 (The Shop)
 * 提供一个复古像素风的“村庄商店”界面。
 * **商品列表：** 包含木剑、铁剑、钻石剑、下界合金剑，以及对应的全套防具。
 * **购买与升级逻辑：** 孩子消耗练习攒下的虚拟金币购买装备。装备成功后，角色的主面板属性（ATK / DEF）会获得永久或阶段性加成。
```
[ 练习获得金币 ] ──> [ 商店购买/升级钻石剑 ] ──> [ 提升 Steve 攻击力 (ATK) ]

```
#### 3.3.4 虚拟战斗机制（每日挑战）
为了让角色的战力提升有处可用，设计一个极简的**异步战斗模拟器**：
 * **怪物图鉴：** 包含僵尸（低血低攻）、苦力怕（高攻低血）、末影人（高血高攻）。
 * **战斗结算：** 每日完成练习后，可消耗一定体能解锁“副本战斗”。
 * **视觉效果需求 (Combat VFX)：** * 无需复杂的 3D 动作渲染，采用 2D 像素帧动画或纯 CSS 动画。
   * 当点击“战斗”时，展示 Steve 与怪物的对峙血条。
   * 根据 Steve 当前的武器和 ATK 属性，屏幕上交替闪烁**“挥剑斩击”的特效光芒（闪烁与震动效果）**，怪物身上弹出红色的伤害飘字（如 -20 HP）。
   * 战斗获胜后，掉落稀有材料或专属荣誉勋章。
## 4. 非功能性需求与系统边界
### 4.1 UI/UX 体验原则
 * **无键盘干扰：** 答题过程中不需要鼠标参与，完全依靠键盘的 Enter 或 iPad 的触屏流转。
 * **大字体设计：** 题目文本不小于 text-2xl (24px)，输入框具备明显的聚焦变色边框。
### 4.2 数据安全与独立性
 * **本地化存储：** 所有历史成绩、错题、游戏角色装备数据仅保存在服务器本地的 SQLite 中，绝不上传至任何第三方云端。
 * **全功能离线可用：** 只要处于家庭局域网内，即使外网断开，平台的所有核心功能（出题、答题、游戏商店）必须 100% 正常运行。
## 5. 待讨论与后续演进功能
 1. **多人联机打卡：** 未来是否支持二胎或同学之间的数据PK，在游戏世界中展示各自 Steve 的装备战力排行榜？
 2. **语文/英语模块的并入：** 下一步规划的词汇听写模块，如何利用 HTML5 的 Web Speech API（语音合成）实现自动读词发音，并复用当前的输入框交互逻辑？
